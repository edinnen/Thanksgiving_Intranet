# Install NodeJS
wget -O - https://raw.githubusercontent.com/audstanley/NodeJs-Raspberry-Pi/master/Install-Node.sh | sudo bash
sudo node-install -v 8.10.0

# Install Yarn
npm -g yarn

# Install pigpio C utilities
sudo apt-get update
sudo apt-get install pigpio

import styled from 'styled-components';

import NormalImg from 'components/Img';

const HelloIcon = styled(NormalImg)`
  width: 24px;
  height: 24px
  transition: all .4s cubic-bezier(0.4,-0.2,0.6,1.2);
`;

export default HelloIcon;
